No extra dependencies required. 
The included scripts use /bin/sh , ip, sed, grep, cut, bc and some sh builtins

1. copy (or link) ./bin/update-hosts to a directory included in $PATH
2. copy  (or link) share/arithmetic-server.sh to /usr/lib/networkd-dispatcher/routable.d or /etc/networkd-dispatcher/routable.d/
   arithmetic-server.sh  file permissions must be 0755 and must be owned by root user and group (uid=0, gid=0)


